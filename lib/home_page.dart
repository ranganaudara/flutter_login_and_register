import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: new Text('Adoooo elama'),),
      body:Center(child: new Container(child: new Text('Welcome broooo!!!'),)),
    );
  }
}